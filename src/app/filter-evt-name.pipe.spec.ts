import { FilterEvtNamePipe } from './pipes/filter-evt-name.pipe';

describe('FilterEvtNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEvtNamePipe();
    expect(pipe).toBeTruthy();
  });
});
